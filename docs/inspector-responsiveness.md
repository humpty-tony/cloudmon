# Responsive event inspection

This draft moves raw-record parsing off the UI thread, bounds field rendering, preserves numeric evidence, and makes detail failures recoverable. See PR #7 for progress and validation. It builds on the search-correctness change group in PR #6.
